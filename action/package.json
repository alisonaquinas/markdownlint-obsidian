{
  "name": "markdownlint-obsidian-action",
  "version": "0.8.0",
  "private": true,
  "description": "GitHub Action wrapper for markdownlint-obsidian",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "bun test --timeout 30000"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
