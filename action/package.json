{
  "name": "markdownlint-obsidian-action",
  "version": "0.8.0",
  "private": true,
  "description": "GitHub Action wrapper for markdownlint-obsidian",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --main-fields=module,main --outfile=dist/main.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "markdownlint-obsidian": "file:.."
  }
}
