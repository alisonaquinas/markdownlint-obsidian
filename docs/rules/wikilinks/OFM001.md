---
title: "OFM001 - Broken wikilink"
aliases:
  - "OFM001 - Broken wikilink"
tags:
  - "wikilinks"
  - "links"
  - "docs"
  - "docs/rules"
  - "docs/rules/wikilinks"
  - "rules/ofm001"
type: "rule-reference"
status: "current"
updated: 2026-07-07
up: "[[rules/index]]"
rule-code: OFM001
rule-name: no-broken-wikilinks
severity: error
fixable: false
area: wikilinks
---

# OFM001 - Broken wikilink

**Severity:** error
**Fixable:** no
**Added in:** Phase 4

## What it does

Resolves every `[[wikilink]]` target against the vault file index. Fires an
error whenever the target cannot be found under Obsidian-compatible resolution:
exact path, case-insensitive path, path suffix, then basename. For example,
`[[sources/foo]]` can resolve to `wiki/sources/foo.md`; bare links like
`[[foo]]` still use basename matching. Embed wikilinks (`![[...]]`) are ignored
— the embed rule family (OFM020+) owns that territory.

## Bad example

```markdown
See [[missing-page]] for details.
```

## Good example

```markdown
See [[existing-page]] for details.
```

## Config

Disable this rule in `.obsidian-linter.jsonc`:

```jsonc
{
  "rules": {
    "OFM001": { "enabled": false }
  }
}
```

You can also suppress the entire wikilink resolution pipeline with
`"resolve": false` or the `--no-resolve` CLI flag.

Legacy path-relative resolution is still available:

```jsonc
{
  "wikilinks": {
    "caseSensitive": false,
    "allowAlias": true,
    "resolveMode": "path-relative"
  }
}
```

## Related

- [[rules/index]]
- [[guides/wikilink-resolution]]
- [[OFM004]] (ambiguous target)
- [[OFM005]] (case mismatch)
