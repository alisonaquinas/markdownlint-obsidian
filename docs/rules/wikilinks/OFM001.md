---
rule-code: OFM001
rule-name: no-broken-wikilinks
severity: error
fixable: false
area: wikilinks
tags: [wikilinks, links]
---

# OFM001 - Broken wikilink

**Severity:** error
**Fixable:** no
**Added in:** Phase 4

## What it does

Resolves every `[[wikilink]]` target against the vault file index. Fires an
error whenever the target cannot be found under any of Obsidian's three
resolution strategies (exact path, case-insensitive path, basename). Embed
wikilinks (`![[...]]`) are ignored — the embed rule family (OFM020+) owns
that territory.

## Bad example

```markdown
See [[missing-page]] for details.
```

## Good example

```markdown
See [[existing-page]] for details.
```

## Config

Disable this rule in `.obsidian-linter.jsonc`:

```jsonc
{
  "rules": {
    "OFM001": { "enabled": false }
  }
}
```

You can also suppress the entire wikilink resolution pipeline with
`"resolve": false` or the `--no-resolve` CLI flag.

## Related

- [[rules/index]]
- [[OFM004]] (ambiguous target)
- [[OFM005]] (case mismatch)
