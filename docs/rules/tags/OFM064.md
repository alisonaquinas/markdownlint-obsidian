---
rule-code: OFM064
rule-name: duplicate-tag
severity: warning
fixable: false
area: tags
tags: [tags]
---

# OFM064 — Duplicate tag

**Severity:** warning
**Fixable:** no
**Added in:** Phase 3

## What it does

Warns when the same tag value appears more than once in body text.
Comparison honours `tags.caseSensitive` (default `false`, so
`#Project` and `#project` collide). The first occurrence is kept;
every subsequent occurrence fires a warning that points back at it.

## Bad example

```markdown
Note #project once and #project again.
```

## Good example

```markdown
Note #project once and #area later.
```

## Config

```jsonc
{
  "tags": { "caseSensitive": true }
}
```

## Related

- [[../index]]
- [[OFM065]]
