---
rule-code: OFM101
rule-name: duplicate-block-id
severity: error
fixable: false
area: block-references
tags: [block-references]
---

# OFM101 - Duplicate block id

**Severity:** error
**Fixable:** no
**Added in:** Phase 6

## What it does

Detects identical `^blockid` declarations inside a single file. Obsidian
renders every duplicate as a single target, so wikilinks to `page#^id`
silently pick the first occurrence — a subtle bug that only surfaces when
readers notice the wrong anchor scrolls into view.

Controlled by `config.blockRefs.requireUnique`. When `false`, the rule is
a no-op even when enabled.

## Bad example

```markdown
First paragraph ^intro

Second paragraph ^intro
```

## Good example

```markdown
First paragraph ^intro

Second paragraph ^summary
```

## Config

Allow duplicates:

```jsonc
{
  "blockRefs": {
    "idPattern": "^[A-Za-z0-9-]{1,32}$",
    "requireUnique": false
  }
}
```

## Related

- [[rules/block-references/OFM100]] (invalid block ref)
- [[rules/block-references/OFM102]] (broken block link)
